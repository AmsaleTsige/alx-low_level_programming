#include <stdio.h>
#include <string.h>
/**
* _puts - Write a function that prints a string, followed
* by a new line, to stdout.
* @str: an input string
* Return: Nothing
*/
void _puts(char *str)
{
puts(str);
}
